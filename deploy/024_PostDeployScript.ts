import { ethers } from 'hardhat';

const PostDeployScript: any = async function () {
  // 1. approve modules
  // 2. set timelock as registry governance
  // 3. eventually change governance from deployer (on Factory etc.)
};

export default PostDeployScript;
